# ─────────────────────────────────────────────────────────────
# SMMS Backend — report-service-build-check
# ─────────────────────────────────────────────────────────────
name: report-service-build-check-pipeline

on:
  push:
    branches:
      - "service/report-service"
    paths:
      - "services/report-service/**"
      - "pom.xml"

  pull_request:
    branches:
      - "dev"
      - "main"
    paths:
      - "services/report-service/**"
      - "pom.xml"
jobs:

  build:

    name: Maven Build Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Give Permissions to Maven in ubuntu
        run: chmod +x mvnw

      #      to see what files are in the project folder
      - name: see files
        run: ls -la

      - name: Build & Test report-service
        run: ./mvnw clean test -f services/report-service -am

      - name: Package report-service
        run: ./mvnw package -DskipTests -f services/report-service -am

      - name: Upload report-service Artifact
        uses: actions/upload-artifact@v4
        with:
          name: report-service-jar
          path: services/report-service/target/*.jar

      - name: Report build status
        if: always()
        run: echo "Build completed with status ${{ job.status }}"

