# ─────────────────────────────────────────────────────────────
# SMMS Backend — config-server-build-check
# ─────────────────────────────────────────────────────────────
name: config-server-build-check-pipeline

on:
  push:
    branches:
      - "service/config-server"
    paths:
      - "services/config-server/**"
      - "pom.xml"

  pull_request:
    branches:
      - "dev"
      - "main"
    paths:
      - "services/config-server/**"
      - "pom.xml"
jobs:

  build:

    name: Maven Build Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      #to see what files are in the project folder
      - name: see files
        run: ls -la

      - name: Build config-server (skip tests for speed)
        run: mvn clean install -pl config-server -am -DskipTests --no-transfer-progress

      - name: Run unit tests
        run: mvn test -pl config-server --no-transfer-progress
        continue-on-error: true   # Don't fail build if tests fail during early development

      - name: Report build status
        if: always()
        run: echo "Build completed with status ${{ job.status }}"

