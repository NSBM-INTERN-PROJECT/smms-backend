# ─────────────────────────────────────────────────────────────
# SMMS Backend — allocation-service-build-check
# ─────────────────────────────────────────────────────────────
name: allocation-service-build-check-pipeline

on:
  push:
    branches:
      - "service/allocation-service"
    paths:
      - "services/allocation-service/**"
      - "pom.xml"

  pull_request:
    branches:
      - "dev"
      - "main"
    paths:
      - "services/allocation-service/**"
      - "pom.xml"
jobs:

  build:

    name: Maven Build Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Give Permissions to Maven in ubuntu
        run: chmod +x mvnw

      #      to see what files are in the project folder
      - name: see files
        run: ls -la

      - name: Build & Test allocation Service
        run: ./mvnw clean test -f services/allocation-service -am

      - name: Package allocation Service
        run: ./mvnw package -DskipTests -f services/allocation-service -am

      - name: Upload allocation Service Artifact
        uses: actions/upload-artifact@v4
        with:
          name: allocation-service-jar
          path: services/allocation-service/target/*.jar

      - name: Report build status
        if: always()
        run: echo "Build completed with status ${{ job.status }}"

